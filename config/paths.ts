const paths = {
  home: {
    index: "/",
    signin: {
      index: "/signin",
      __query: ["signin"],
    },
    profile: {
      index: "/profile",
      __query: ['profile'],
    }
  },
};

export default paths;
